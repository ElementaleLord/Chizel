Go to discord and download the .env file sent, then proceed with these steps

1. Add the .env in the main directory (next to README.md)
2. Open integrated terminal (on the directory)
3. run the command <export DB_KEY="replace-value-here"> (ofc without <>)